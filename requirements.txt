# ─────────────────────────────────────────────────────────────
# Mask2Flow-TSE — Full Requirements
# Install PyTorch with CUDA FIRST (see README), then run:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────

# Core ML (install with CUDA separately — see above)
torch>=2.1.0
torchaudio>=2.1.0
numpy>=1.24.0
scipy>=1.10.0

# Audio processing
soundfile>=0.12.0
librosa>=0.10.0

# Speaker encoder (WavLM)
transformers>=4.35.0
huggingface-hub>=0.19.0

# ASR evaluation (Whisper)
openai-whisper>=20231117

# Config management
pyyaml>=6.0
omegaconf>=2.3.0

# Training utilities
einops>=0.7.0
timm>=0.9.0

# Logging
tensorboard>=2.14.0
tqdm>=4.65.0

# Visualization
matplotlib>=3.7.0

# Data handling
pandas>=2.0.0

# Speaker similarity evaluation
# wespeaker installed separately:
# pip install wespeaker